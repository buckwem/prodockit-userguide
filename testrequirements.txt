# Copyright (c) 2025-2026 Mark Buckwell and contributors
# SPDX-License-Identifier: MIT

# The test suite's own prerequisites (see test/), kept separate from
# requirements.txt so someone only building the docs never needs them.
# Install alongside requirements.txt:
#   pip install -r requirements.txt -r testrequirements.txt

# Brings pytest, pymupdf, and prodockit.testing's own pytest plugin - the
# prodockit_* fixtures this suite uses for the built PDF, plus the shared
# Mermaid/TeX rendering checks. Replaces the loose pytest/pymupdf pins that
# were here, and the local copy of those checks (see #27).
prodockit[testing]>=0.60.1
